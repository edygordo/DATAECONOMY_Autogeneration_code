{
    "dependencies": ["."],
    "graphs":{
        "Automatic_Codegenerator": "./graph/graph.py:app"
    },
    "env": ".env",
    "dockerfile_lines": [
        "RUN apt-get update && apt-get install -y gcc"
    ]
}